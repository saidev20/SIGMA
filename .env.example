# Vite frontend environment variables
# Copy this file to `.env` and adjust as needed.

# Base URL for backend FastAPI server (no trailing slash). Used for REST and WS.
VITE_BACKEND_URL=http://localhost:5000
